import { Link } from "react-router-dom";

const Logo = () => {
  return (
    <Link to="/">
      <p className="font-bebasNeue text-5xl uppercase text-white">Faun</p>
    </Link>
  );
};

export default Logo;
